import React from 'react';
import classes from './Map.module.css';

const Map = (props) => { 
    return (
        <div className={classes.map}>
            Map of where I am 
        </div>
    )
}
export default Map;